import { css } from "@styled-system/css";

export default function Icon() {
  return (
    <div
      className={css({
        width: "20px",
        height: "20px",
        backgroundColor: "gray",
      })}
    />
  );
}
